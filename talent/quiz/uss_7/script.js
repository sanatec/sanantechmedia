const questions = [
{
        question: "صحیح جملہ چن لیجیے؟ ",
        options: ["یہ کالے چھتری ہے۔", "یہ کالی چھتری ہے۔", "یہ کالا چھتری ہے۔", "اوپر میں سے کوئی نہیں۔"],
        correct: 1,
      },
{
        question: " میری  اچّھی     پیاری      تتلی۔اوپر کے مصرعہ میں کتنے حروف ہیں ؟",
        options: ["سترہ", "اٹھارہ", "انّیس", "بیس"],
        correct: 1,
      },
{
        question: "صحیح جملہ چن لیجیے؟ ",
        options: ["آپ  کہاں جاتے ہو۔", "آپ کہاں  جاتا ہوں ۔", "آپ کہاں جاتے ہیں ", "آپ  کہاں جاتا ہے ۔"],
        correct: 2,
      },
{
        question: "  صحیح لفظ  چن لیجیے؟ ",
        options: ["ضرورت", "زرووت", "ذرورت", "ظرورت"],
        correct: 0,
      },
{
        question: "صحیح جملہ چن لیجیے؟ ",
        options: [" میں اردو پڑھتا ہے۔", " میں اردو پڑھتا ہیں۔", "میں اردو پڑھتا ہوں ۔", " میں اردو پڑھتا ہو۔"],
        correct: 2,
      },
{
        question: " اکتوبر کے  مہینے میں کتنے دن  ہیں ؟ ",
        options: ["30", "29", "31", "28"],
        correct: 2,
      },
{
        question: " اکتوبر ایک منگل ہے تو 31 اکتوبر کون سا دن ہے؟ ",
        options: [" منگل", " بدھ", "جمعرات", " جمعہ"],
        correct: 2,
      },
{
        question: " دو  کلو سیب  کے  دام دو سو  پچاس ہیں  تو ایک کلو کے  دام کتنے ہیں ؟",
        options: [" دو سو", " ایک سو پچیس", " ایک سو پچاس", " دو سو پچاس"],
        correct: 1,
      },
{
        question: "'۲۵ ' کو  اردو میں کیا کہتے ہیں ؟ ",
        options: [" پچاس", "پچیس", "پینتیس", "انچاس"],
        correct: 1,
      },
{
        question: "  پنساری کی دکان سے تعلق ہیں ؟",
        options: [" کیلا اور سنترہ", " گیہوں  اور نمک", " کدّو اور پیاز", "حلوہ اور مٹھائی"],
        correct: 1,
      },
{
        question: "پٹری پر چلنے والی ایک گاڑی ",
        options: ["جہاز", "سائیکل", " ریل گاڑی", " بیل گاڑی"],
        correct: 2,
      },
{
        question: "نیچے دیے جانوروں میں لمبی گردن کس کی ہے؟ ",
        options: [" ہاتھی", "بندر", "زرافہ", "چیتا"],
        correct: 2,
      },
{
        question: " پیشہ سے تعلق رکھنے والے مناسب  جملے چن لیجیے؟ ",
        options: ["درزی  مٹی کے برتن بناتا ہے ۔", " کمہار کپڑے سلتا ہے۔", " کسان  مچھلی پکڑتا ہے۔", " سنار سونے کے زیور بناتا ہے۔"],
        correct: 3,
      },
{
        question: "صحیح جملہ چن لیجیے؟ ",
        options: [" گرمی کا موسم تھی۔", "گرمی کی موسم تھا۔", "گرمی کے موسم تھی۔", "گرمی کا موسم تھا۔"],
        correct: 3,
      },
{
        question: "'پیڑ لگاؤ فطرت بچاؤ'۔اوپر کے الفاظ    کیا  ہے ؟ ",
        options: [" اعلان", " نعرہ", " معمہ", "نعت"],
        correct: 1,
      },
{
        question: " سورج کہاں سے نکلتاہے؟ ",
        options: ["مغرب ", " مشرق", "جنوب", "شمال"],
        correct: 1,
      },
{
        question: "صحیح جملہ چن لیجیے؟ ",
        options: ["چھٹی کا دن تھی", " چھٹی کے دن تھا۔", " چھٹی کا دن تھا۔", " چھٹی کی  دن تھی۔"],
        correct: 2,
      },
{
        question: "  ہم ان کے مدد گار اور وہ ہمارے۔یہ شعر کا شاعر کون ہے؟",
        options: [" الطاف حسین حالی", " ظفر گور کھپوری", "مرازا غالب", " ایس ایم سرور"],
        correct: 1,
      },
{
        question: "  بڑے سیدھے سادے ہمارے پڑوسی۔شعر کا شاعر کون ہے ؟",
        options: ["	میر ", "اقبال", "حالی ", "ظفر گور کھپوری"],
        correct: 3,
      },
{
        question: " 'بلائیں '   کا ہم آواز نہیں    ہے؟ ",
        options: ["سلائیں ", "بتائیں", "کرائیں", " دیکھیں"],
        correct: 3,
      },
{
        question: " امّی کی امّی کو کیا کہتی ہیں ؟ ",
        options: ["نانی امّاں", " دادی امّاں", " سالی امّاں", "خالی امّاں"],
        correct: 0,
      },
{
        question: "  ابّا کے ابّا کو کیا کہتے ہیں؟",
        options: ["دادا", "نانا ", "چاچا ", "بھیّا"],
        correct: 0,
      },
{
        question: "کوڈومباشری .....انجمن ہے۔ ",
        options: [" نوجوانوں کی", "بچوّں کی", " عورتوں کی", "مردوں کی "],
        correct: 2,
      },
{
        question: "  کوڈومباشری کا اہم مقصد ",
        options: ["صحت مند ہونا ", "غریبی دور کرنا ", " بہت پیسا کمانا", " تعلیم حاصل کرنا"],
        correct: 1,
      },
{
        question: " قومی جھنڈے کی بیچ میں کون سے رنگ ہے؟",
        options: ["ہرا", "کالا", "سفید", "زعفرانی"],
        correct: 2,
      },    
];

let currentQuestion = 0;
let correctAnswers = 0;
let userAnswers = new Array(questions.length).fill(-1); // Track user selections

const correctSound = document.getElementById("correct-sound");
const incorrectSound = document.getElementById("incorrect-sound");
const backButton = document.getElementById("back");
const nextButton = document.getElementById("next");

function loadQuestion() {
    const questionElement = document.getElementById("question");
    const optionsContainer = document.getElementById("options");

    questionElement.textContent = questions[currentQuestion].question;
    optionsContainer.innerHTML = "";

    questions[currentQuestion].options.forEach((option, index) => {
        const button = document.createElement("button");
        button.textContent = option;
        button.onclick = () => checkAnswer(index, button);
        optionsContainer.appendChild(button);

        // If already answered, highlight accordingly
        if (userAnswers[currentQuestion] !== -1) {
            button.disabled = true;
            if (index === questions[currentQuestion].correct) {
                button.classList.add("correct");
            }
            if (index === userAnswers[currentQuestion] && index !== questions[currentQuestion].correct) {
                button.classList.add("wrong");
            }
        }
    });

    // Update navigation buttons
    backButton.disabled = currentQuestion === 0;
    nextButton.disabled = userAnswers[currentQuestion] === -1 && currentQuestion < questions.length - 1;
    if (currentQuestion === questions.length - 1 && userAnswers[currentQuestion] !== -1) {
        nextButton.textContent = "Finish";
    } else {
        nextButton.textContent = "Next";
    }
}

function checkAnswer(selectedIndex, selectedButton) {
    if (userAnswers[currentQuestion] !== -1) return; // Already answered

    const correctIndex = questions[currentQuestion].correct;
    const buttons = document.querySelectorAll(".options button");

    userAnswers[currentQuestion] = selectedIndex;

    // Highlight correct answer always
    buttons[correctIndex].classList.add("correct");

    if (selectedIndex === correctIndex) {
        correctAnswers++;
        document.getElementById("score").textContent = correctAnswers;
        correctSound.play();
        showConfetti();
    } else {
        selectedButton.classList.add("wrong");
        incorrectSound.play();
    }

    // Disable all buttons
    buttons.forEach((button) => (button.disabled = true));

    // Enable next button
    nextButton.disabled = false;
}

function showConfetti() {
    const colors = ['#f00', '#0f0', '#00f', '#ff0', '#f0f', '#0ff'];
    for (let i = 0; i < 100; i++) {
        const confetti = document.createElement('div');
        confetti.className = 'confetti';
        confetti.style.left = Math.random() * 100 + 'vw';
        confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
        confetti.style.animationDuration = Math.random() * 3 + 2 + 's';
        confetti.style.opacity = Math.random() + 0.5;
        document.body.appendChild(confetti);

        setTimeout(() => {
            confetti.remove();
        }, 3000);
    }
}

backButton.onclick = () => {
    if (currentQuestion > 0) {
        currentQuestion--;
        loadQuestion();
    }
};

nextButton.onclick = () => {
    if (userAnswers[currentQuestion] === -1) return; // Not answered yet

    if (currentQuestion < questions.length - 1) {
        currentQuestion++;
        loadQuestion();
    } else {
        showResults();
    }
};

function showResults() {
    const modal = document.getElementById("results-modal");
    const correctBar = document.getElementById("correct-bar");
    const incorrectBar = document.getElementById("incorrect-bar");
    const correctCount = document.getElementById("correct-count");
    const incorrectCount = document.getElementById("incorrect-count");
    const resultsMessage = document.getElementById("results-message");

    const totalQuestions = questions.length;
    const incorrectAnswers = totalQuestions - correctAnswers;

    correctCount.textContent = correctAnswers;
    incorrectCount.textContent = incorrectAnswers;

    correctBar.style.width = `${(correctAnswers / totalQuestions) * 100}%`;
    incorrectBar.style.width = `${(incorrectAnswers / totalQuestions) * 100}%`;

    if (correctAnswers === totalQuestions) {
        resultsMessage.textContent = "Congratulations! You answered all questions correctly!";
    } else {
        resultsMessage.textContent = "Try Again!";
    }

    modal.style.display = "flex";
}

function showReview() {
    const reviewQuestions = document.getElementById("review-questions");
    reviewQuestions.innerHTML = "";

    questions.forEach((q, idx) => {
        const reviewDiv = document.createElement("div");
        reviewDiv.className = "review-question";

        const questionH3 = document.createElement("h3");
        questionH3.textContent = `${idx + 1}. ${q.question}`;
        reviewDiv.appendChild(questionH3);

        const optionsDiv = document.createElement("div");
        optionsDiv.className = "review-options";

        q.options.forEach((option, optIdx) => {
            const button = document.createElement("button");
            button.textContent = option;
            button.disabled = true;

            if (optIdx === q.correct) {
                button.classList.add("correct");
            }
            if (optIdx === userAnswers[idx] && optIdx !== q.correct) {
                button.classList.add("wrong");
            } else if (optIdx === userAnswers[idx]) {
                button.classList.add("correct"); // If correct, already added
            }

            optionsDiv.appendChild(button);
        });

        reviewDiv.appendChild(optionsDiv);
        reviewQuestions.appendChild(reviewDiv);
    });

    document.getElementById("review-modal").style.display = "flex";
}

function closeReview() {
    document.getElementById("review-modal").style.display = "none";
}

function restartQuiz() {
    currentQuestion = 0;
    correctAnswers = 0;
    userAnswers.fill(-1);
    document.getElementById("score").textContent = correctAnswers;
    document.getElementById("results-modal").style.display = "none";
    loadQuestion();
}

// Load the first question
loadQuestion();
